# Unreal Engine
Binaries/
Intermediate/
Saved/
DerivedDataCache/
*.sln
*.suo
*.opensdf
*.sdf
*.VC.db
*.VC.VC.opendb

# Builds
*.xcodeproj

# OS
.DS_Store
Thumbs.db

# Logs (root or stray)
*.log

# Content-derived caches (if present later)
Content/DerivedDataCache/

# IDE / tooling
.vs/
UpgradeLog.htm
*.vcxproj.user
*.sublime-*

# External plugins (installed via tools, not project code)
Plugins/UnrealMCP/

# Python
__pycache__/
*.pyc

# Secrets and debug logs
.env
.env.local
.env.*.local

# Cursor MCP live config (may hold machine paths / credentials).
# Start from .cursor/mcp.json.example — see docs/Setup/MCP_SETUP.md
.cursor/mcp.json
.mcp.json
# .devenv workspace (auto-added)
.devenv/

# KEEP-LOCAL Mannequins (Docs/17e HS-E) — Epic TP High Characters copy per DESKTOP;
# never commit. Cold clone: copy from UE_5.8 TemplateResources (see Docs/17e).
Content/Characters/
