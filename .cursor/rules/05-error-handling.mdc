---
name: "Error Handling Standards"
description: "Defensive programming and error handling patterns"
alwaysApply: true
priority: 6
---

# Defensive Programming

## Error Handling Philosophy

- Assume all external inputs can be invalid, null, undefined, or malicious
- Assume all network calls can fail, timeout, or return unexpected data
- Assume all file operations can fail (permissions, disk full, path doesn't exist)
- Assume all user input needs validation and sanitization

## Patterns

### 1. Null/Undefined Checks

```typescript
// Good: Explicit null check
if (!data || !data.user) {
  throw new Error('User data is required');
}

// Bad: Assumes data exists
const name = data.user.name;
```

### 2. Try-Catch with Context

```typescript
// Good: Error includes context
try {
  await processFile(path);
} catch (error) {
  throw new Error(`Failed to process ${path}: ${error.message}`);
}

// Bad: Generic error
try {
  await processFile(path);
} catch (error) {
  throw error;
}
```

### 3. Validation at Boundaries

- Validate at function entry points
- Validate at API boundaries
- Validate at data transformation boundaries
- Never trust data from external sources

### 4. Graceful Degradation

- Provide fallbacks for non-critical features
- Log errors but don't crash the application
- Return meaningful error messages to users
- Use default values when safe

### 5. Edge Cases to Always Consider

- Empty arrays/strings/objects
- Very large numbers (overflow)
- Negative numbers where unexpected
- Special characters in strings
- Unicode/emoji in text
- Concurrent access/modification
- Network timeouts
- Partial failures in batch operations

## Learning from Errors

- Document errors and their fixes so the same mistake is not repeated.
- Prefer a single project-wide place (e.g. `docs/KNOWN_ERRORS.md` or `docs/LESSONS_LEARNED.md`) for recurring or instructive errors.
- Follow the process in `07-ai-agent-behavior.mdc` (error recurrence prevention) when working in the codebase.

## UE 5.7 Migration Guard

Before writing or reviewing C++ that touches engine APIs, check for deprecated patterns:

- **Read `docs/KNOWN_ERRORS.md`** and the pitfalls table in `unreal-cpp.mdc` before using `FindObject`, image utilities, or global constants.
- **Do not use `ANY_PACKAGE`** — it was removed in UE 5.7. Use `nullptr`.
- **Avoid generic global names** (e.g. `BufferSize`) that collide with engine headers; prefix and use `static constexpr`.
- When integrating third-party plugins targeting older UE versions, expect API breakage and apply known fixes before building.

## MCP Input Validation

Before calling any MCP tool, validate the arguments to prevent Editor crashes:

- **`blueprint_name`:** Must be a short name only (e.g. `BP_GameMode`). Never pass a full `/Game/...` asset path — the plugin prepends `/Game/Blueprints/` causing a double-slash fatal crash.
- **`property_name`:** Inherited C++ UPROPERTY names may not resolve. If `set_blueprint_property` returns "Property not found", fall back to Python Editor scripts.
- **`component_name`:** C++ constructor-created components (e.g. `CharacterMesh0`) are invisible to MCP. Use Python scripts instead.
- **Always check `docs/KNOWN_ERRORS.md`** before using MCP tools on new Blueprint types.

## Concurrency & Async Patterns

**Async/Await:**
- Prefer async/await over callbacks
- Handle errors with try-catch
- Don't forget `await` (use linter rules)
- Use `Promise.all()` for parallel operations
- Use `Promise.allSettled()` when some failures are acceptable

**Concurrency Control:**
- Limit concurrent operations (worker pools)
- Use semaphores/locks when needed
- Handle race conditions
- Test concurrent code thoroughly
- Use atomic operations when possible

**Error Handling:**
- Always handle async errors
- Don't swallow errors silently
- Propagate errors appropriately
- Use error boundaries (frontend)

**Concurrency Checklist:**
- [ ] Async errors are handled
- [ ] Concurrent operations are controlled
- [ ] Race conditions prevented
- [ ] Timeouts configured
- [ ] Resource cleanup on errors
