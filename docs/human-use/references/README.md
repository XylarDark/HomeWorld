# Human reference implementations

Not a gate. Drop golden, human-written snippets here so a skill has something real to
copy. The agent must not seed this folder with code it just generated.

Name files after the task type they illustrate (`new-panel.md`, `http-handler.ts`,
`game-module.md`). Point at the file from [test-contract.md](../test-contract.md).
