# HomeWorld – Console commands reference

Short reference for all `hw.*` and related console commands used in PIE testing and automation. All commands require a **play world** (PIE or packaged game); they are registered as cheat commands (`ECVF_Cheat`). Source: `Source/HomeWorld/HomeWorld.cpp`, `Source/HomeWorld/HomeWorldTimeOfDaySubsystem.cpp`.

**Pre-demo verification (one doc):** Run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this doc ([§ Pre-demo verification](#pre-demo-verification-entry-point) and [Commands](#commands-hw)). **List 63 integration** (run order and where to document): [§ List 63 integration](#list-63-integration). Open this document for both §3 and the command reference. **First-launch flow and main menu flow (List 55):** See [CHARACTER_GENERATION_AND_CUSTOMIZATION §2](CHARACTER_GENERATION_AND_CUSTOMIZATION.md#2-main-menu-and-character-screen-setup) (First-launch flow) and the [main menu flow checklist](CHARACTER_GENERATION_AND_CUSTOMIZATION.md#main-menu-flow-checklist) (MainMenu → Play, Character, Options, Quit) for how to verify game start and main menu buttons.

**Fifty-first list refresh (2026-03-07):** This doc and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) were refreshed for the current PIE flow (MVP tutorial + post-tutorial + Week 1 playtest); pre-demo and tutorial verification steps are up to date.

**Fifty-second list (T1 — pre-demo verification entry point):** This document is the **single entry point** linking (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS.

**Fifty-second list (T6 — doc freshness):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) refreshed for post–T1–T5 state (refinement, pie_test_runner, demo run/sign-off, sin/virtue or planetoid polish, §4 fifty-second deliverables). No new commands this list; entry point and command reference remain current.

**Fifty-third list (T1 — pre-demo verification entry point):** This document remains the **single entry point** linking (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS.

**Fifty-fourth list (T6 — demo run/sign-off):** This document is the **single entry point** linking §3 (run sequence) and the command reference. T6 demo steps run: pre-demo sequence executed (level open, pie_test_runner.py via MCP); outcome in VERTICAL_SLICE_CHECKLIST §3 and SESSION_LOG. Optional recording deferred; slice demo-ready per §3.2 when PIE + corner/moment verified.

**Fifty-fourth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state. Entry point unchanged: this doc links §3 (run sequence) and the command reference. Fifty-fourth-list deliverables table added to VERTICAL_SLICE_CHECKLIST §4; cycle note in KNOWN_ERRORS.

**Fifty-fifth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (main menu flow, first-launch flow per List 55). Entry point unchanged: this doc links §3 (run sequence) and the command reference. First-launch and main menu checklist: [CHARACTER_GENERATION_AND_CUSTOMIZATION §2](CHARACTER_GENERATION_AND_CUSTOMIZATION.md#2-main-menu-and-character-screen-setup) and [main menu flow checklist](CHARACTER_GENERATION_AND_CUSTOMIZATION.md#main-menu-flow-checklist). Vertical slice §4 fifty-fifth deliverables confirmed; cycle note in KNOWN_ERRORS.

**Fifty-sixth list (T5 — bed in-world verification):** In-world bed verification steps (go-to-bed and wake in PIE via interact or overlap) are in § Pre-demo verification **Tutorial (List 8) verification** and **Tutorial (List 10) verification**. This document remains the **single entry point** linking (1) the run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc. Open this doc for both §3 and CONSOLE_COMMANDS.

**Fifty-sixth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (bed in-world, go-to-bed, wake per List 56). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Fifty-seventh list (T5 — in-world meal verification):** In-world meal verification (breakfast/lunch/dinner) added to § Tutorial (List 3) verification (breakfast already present) and § Tutorial (List 7) verification (in-world lunch and dinner). MVP_TUTORIAL_PLAN checklist rows for steps 2, 6, 7 and List 3/7 scope updated to include in-world options. Entry point unchanged: this doc links §3 (run sequence) and the command reference.

**Fifty-seventh list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (in-world meal triggers List 57). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Fifty-eighth list (T5 — love task in-world verification):** In-world love task verification (interact with partner) is in § Tutorial (List 4) verification (in-world verification and placement; List 58 T3). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Open this doc for both §3 and CONSOLE_COMMANDS.

**Fifty-eighth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (in-world love task trigger List 58). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Fifty-ninth list (T5 — game-with-child in-world verification):** In-world game-with-child verification (interact with child) is in § Tutorial (List 5) verification (in-world verification and placement; List 59 T3). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Open this doc for both §3 and CONSOLE_COMMANDS.

**Fifty-ninth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (in-world game-with-child trigger, interact with child per List 59). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixtieth list (T1 — build-order claim and completion path):** The **build-order claim and completion path** (family agent claims and completes one build order) is defined in [AGENTIC_BUILDING.md § Build-order claim and completion path](tasks/AGENTIC_BUILDING.md#build-order-claim-and-completion-path-t1--list-60). Verification: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands for agentic building → this doc (hw.PlaceWall, hw.CompleteBuildOrder, hw.SimulateBuildOrderActivation). Open this doc for both §3 and CONSOLE_COMMANDS.

**Sixtieth list (T5 — agentic building verification):** This document and [MVP_TUTORIAL_PLAN.md](workflow/MVP_TUTORIAL_PLAN.md) include **agentic building (List 60)** verification: **hw.PlaceWall**, **hw.CompleteBuildOrder**, **hw.SimulateBuildOrderActivation** and how to verify "family agent completes one build order" in PIE. See § Agentic building (List 60) verification below and [AGENTIC_BUILDING.md](tasks/AGENTIC_BUILDING.md), [DAY10_AGENTIC_BUILDING.md](tasks/DAY10_AGENTIC_BUILDING.md).

**Sixtieth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (agentic building List 60). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixty-first list (T1 — astral-by-day entry path):** **hw.EnterAstral** and **hw.AstralByDay** added for List 61 (enter astral during day). From Day or Dusk, phase becomes Night; return with hw.AstralDeath (or F8) restores Day. See § Commands and § Key PIE-test usage (Astral-by-day). Entry point unchanged: this doc links §3 (run sequence) and the command reference.

**Sixty-first list (T5 — astral-by-day verification):** This document and [MVP_TUTORIAL_PLAN.md](workflow/MVP_TUTORIAL_PLAN.md) include **astral-by-day (List 61)** verification: **hw.EnterAstral**, **hw.AstralByDay**, and how to trigger and verify "enter astral during day" in PIE. See § Astral-by-day (List 61) verification below and [ASTRAL_DEATH_AND_DAY_SAFETY.md](tasks/ASTRAL_DEATH_AND_DAY_SAFETY.md) §3.

**Sixty-first list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (astral-by-day List 61). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixty-second list (T1 — Defend-at-night flow):** **Defend-at-night (List 62)** verification added: **place_defend_gather_positions.py** places DefendPosition- and GatherPosition-tagged actors; with **Family**-tagged actors (e.g. from place_partner/place_child), PIE **hw.TimeOfDay.Phase 2** → family move to Defend, **Phase 0/3** → return at dawn. See § Defend-at-night (List 62) verification below and [DAY12_ROLE_PROTECTOR.md](tasks/DAY12_ROLE_PROTECTOR.md).

**Sixty-second list (T5 — Defend-at-night verification doc):** [CONSOLE_COMMANDS.md](CONSOLE_COMMANDS.md) and [MVP_TUTORIAL_PLAN.md](workflow/MVP_TUTORIAL_PLAN.md) include **Defend-at-night (List 62)** verification: **hw.TimeOfDay.Phase 0/2/3**, family at Defend at night, return at dawn; checklist row and List 62 scope in MVP_TUTORIAL_PLAN. Entry point unchanged: this doc links §3 (run sequence) and the command reference. Open this doc for both §3 and CONSOLE_COMMANDS.

**Sixty-second list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (Defend-at-night List 62). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixty-third list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 63 integration: it links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Use this doc for the **tutorial loop single-session run** (MVP_TUTORIAL_PLAN 13 steps), **Week 1 playtest** (crash → scout → boss → claim home), and **pre-demo checklist** run. Open this doc for both §3 and CONSOLE_COMMANDS.

**Sixty-third list (T2 — Week 1 playtest single-session run):** Week 1 playtest run was **deferred** (Editor/MCP not connected). When Editor is available: run §3 sequence then the **Week 1 playtest checklist** in this doc (§ Pre-demo verification); document outcome in SESSION_LOG or [DAY5_PLAYTEST_SIGNOFF § T1 verification](tasks/DAY5_PLAYTEST_SIGNOFF.md#t1-verification-current_task_list--week-1-playtest-loop).

**Sixty-third list (T3 — vertical slice pre-demo checklist run):** Pre-demo checklist §3 run was **deferred** (Editor/MCP not connected). When Editor is available: follow [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) steps 1–8 (open DemoMap, PCG generated, start PIE, run `pie_test_runner.py`, inspect `Saved/pie_test_results.json`); document pass/fail and any gaps in SESSION_LOG or VERTICAL_SLICE_CHECKLIST §3.

**Sixty-third list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 63 integration: tutorial loop, Week 1 playtest, pre-demo checklist; run order and outcome locations in § List 63 integration). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixty-fourth list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 64 (packaged build smoke-test, demo sign-off, MVP full-scope verification): it links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS. List 64 is the final list of the MVP full scope (10 lists).

**Sixty-fourth list (T4 — CONSOLE_COMMANDS and checklist refresh):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect List 64 scope: **packaged build** (run [Package-HomeWorld.bat](../../Package-HomeWorld.bat) when Editor is closed; document outcome in SESSION_LOG or [KNOWN_ERRORS.md](KNOWN_ERRORS.md); see KNOWN_ERRORS "Package-HomeWorld"), **demo sign-off** (follow §3 steps 1–8, then §3.2 items 1–3 when Editor available; document in SESSION_LOG or VERTICAL_SLICE_CHECKLIST §3), and **MVP full-scope verification** (tutorial loop, Week 1 playtest, pre-demo per § List 63 integration). Run order: pre-demo → §3 steps 1–8 + pie_test_runner + Saved/pie_test_results.json; demo sign-off → §3.2 (1)–(3); packaged build → separate run with Editor closed. Entry point unchanged: this doc links §3 (run sequence) and the command reference.

**Sixty-fourth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 64: packaged build, demo sign-off, MVP full-scope verification). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md).

**Sixty-fifth list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 65 (Harden & demo): it links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS. Vertical slice lock: moment = Claim homestead, corner = Homestead compound (locked in [PROTOTYPE_SCOPE.md](workflow/PROTOTYPE_SCOPE.md)).

**Sixty-fifth list (T4 — CONSOLE_COMMANDS and checklist refresh):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) refreshed for post–List 64 / sixty-fifth list (Harden & demo). Entry point and run order are current: pre-demo → §3 steps 1–8 + pie_test_runner + Saved/pie_test_results.json; demo sign-off → §3.2 (1)–(3) when Editor available. No new commands this list. Entry point unchanged: this doc links §3 (run sequence) and the command reference. T4 status set to completed.

**Sixty-fifth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 65 Harden & demo: vertical slice lock, pre-demo checklist, demo sign-off, CONSOLE_COMMANDS refresh). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md). T8 status set to completed.

**Sixty-sixth list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 66 (MVP 100 Phase 1: Run and verify). It links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Use this doc for **Phase 1.1** (PIE + pie_test_runner), **Phase 1.2** (tutorial loop single-session), **Phase 1.3** (Week 1 playtest), and **Phase 1.4** (pre-demo checklist). Open this doc for both §3 and CONSOLE_COMMANDS. See [MVP_100_PHASED_APPROACH.md](workflow/MVP_100_PHASED_APPROACH.md) Phase 1.

**Sixty-sixth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 66 MVP 100 Phase 1: PIE + pie_test_runner, tutorial loop, Week 1 playtest, pre-demo checklist; T1–T7 completed; Phase 1.1–1.4 runs deferred with procedure when Editor available). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md). T8 status set to completed.

**Sixty-seventh list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 67 (MVP 100 Phase 2: Packaged build run and smoke-test). It links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Phase 2 adds packaged build (run [Package-HomeWorld.bat](../../Package-HomeWorld.bat) when Editor closed; see [KNOWN_ERRORS.md](KNOWN_ERRORS.md) "Package-HomeWorld") and optional smoke-test of staged exe; pre-demo/PIE verification remains §3 + this doc. Open this doc for both §3 and CONSOLE_COMMANDS.

**Sixty-seventh list (T7 — Phase 2 alignment):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect Phase 2 outcome: packaged build run (outcome in SESSION_LOG/KNOWN_ERRORS); smoke-test deferred (no exe at StagedBuilds; path and retry in [STEAM_EA_STORE_CHECKLIST.md](workflow/STEAM_EA_STORE_CHECKLIST.md)). No doc update needed; entry point and §3 remain current for Phase 3 (Demo sign-off).

**Sixty-seventh list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 67 MVP 100 Phase 2: packaged build run, smoke-test deferred; Phase 3 = Demo sign-off). Entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md). T8 status set to completed.

**Sixty-eighth list (T1 — pre-demo verification entry point):** This document is the **single entry point** for List 68 (MVP 100 Phase 3: Demo sign-off). It links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS. Phase 3.1: confirm moment (Claim homestead) and corner (Homestead compound) per [PROTOTYPE_SCOPE.md](workflow/PROTOTYPE_SCOPE.md); spot-check in PIE when Editor available; document confirmation in VERTICAL_SLICE_CHECKLIST §3.

**Sixty-eighth list (T7 — Phase 3 alignment):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect Phase 3 outcome (moment/corner confirmed in §3; demo sign-off documented; entry point unchanged). No doc update needed; this doc remains the single entry point linking §3 (run sequence) and the command reference. T7 status set to completed.

**Sixty-eighth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 68 MVP 100 Phase 3: Demo sign-off). Vertical slice §4 sixty-eighth deliverables confirmed (T5); entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md). T8 status set to completed.

**Sixty-ninth list (T8 — docs and cycle):** CONSOLE_COMMANDS and [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) reflect current state (List 69 MVP 100 Phase 4: 100% MVP asset-ready gate). Vertical slice §4 sixty-ninth deliverables confirmed (T6); entry point unchanged: this doc links §3 (run sequence) and the command reference. Cycle note in [KNOWN_ERRORS.md](KNOWN_ERRORS.md). T8 status set to completed.

**Seventieth list (T1 — pre-demo verification entry point):** This document is the **single entry point** linking (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this doc (§ Pre-demo verification and Commands). Open this doc for both §3 and CONSOLE_COMMANDS. See also [PROJECT_STATE_AND_TASK_LIST.md](workflow/PROJECT_STATE_AND_TASK_LIST.md) §1 (Pre-demo verification entry point).

**Seventieth list (T4 — bed actor):** In-world bed is **implemented**. **BP_Bed** (`/Game/HomeWorld/Building/`): create with **create_bp_bed.py**, place on DemoMap with **place_bed.py** (open DemoMap, run via Tools → Execute Python Script or MCP `execute_python_script("place_bed.py")`). **Interact (E)** at bed or **overlap** (optional HomeWorldGoToBedTriggerComponent) triggers go-to-bed (phase → Night) or wake (Night → Dawn). Verification: § Tutorial (List 8) and § Tutorial (List 10) (List 56).

**Seventieth list (T7 — Deferred-phase outcomes):** List 70 (Deferred features) outcomes — **implemented:** SaveGame/role persistence (hw.Save, hw.Load, hw.Roles; DAY15 §4); boss reward (hw.GrantBossReward; HUD toast, Wood); in-world bed (BP_Bed, place_bed.py; interact/overlap go-to-bed and wake; § Tutorial List 8/10); spirit roster on death (hw.ReportDeath, **hw.Spirits** list roster; pie_test_runner check_report_death). **Deferred:** Full agentic Path 2 (State Tree BUILD branch; Path 1 hw.PlaceWall, hw.CompleteBuildOrder, hw.SimulateBuildOrderActivation works). **Pre-demo verification:** This doc is the single entry point: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this doc (§ Pre-demo verification and Commands). New verification steps this list: **hw.Spirits** (list spirit roster after hw.ReportDeath); bed interact/overlap (§ Tutorial List 8, List 10). See [PROJECT_STATE_AND_TASK_LIST.md §2 Deferred features](workflow/PROJECT_STATE_AND_TASK_LIST.md#deferred-features-track-so-we-dont-re-do-verifydocument-every-list) for status and next-list rules.

---

<a id="list-63-integration"></a>
## List 63 integration (run order and outcome locations)

**Integration verification (List 63):** To verify the full integration in one or two sessions, use the **run order** in the table below; **commands** and step-by-step verification are in [§ Pre-demo verification](#pre-demo-verification-entry-point) and [§ Commands (hw.*)](#commands-hw); **record pass/fail** in the "Where to document results" column for each run.

**Single entry point:** This document is the single entry point for List 63 integration runs. It links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing), (2) the tutorial checklist → [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) (13 steps), and (3) the Week 1 playtest checklist → this doc (§ Pre-demo verification — Week 1 playtest checklist). Open this doc for §3 and the command reference.

**Order of runs (List 63):**

| Order | Run | Checklist / procedure | Expected outcome | Where to document results |
|-------|-----|------------------------|------------------|----------------------------|
| 1 | Tutorial loop single-session | [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) 13 steps; commands in this doc (§ Pre-demo verification, Tutorial List 2–10) | All 13 steps completed or documented partial run (which completed, deferred, failed) | [SESSION_LOG](../SESSION_LOG.md) or [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) |
| 2 | Week 1 playtest single-session | This doc § Pre-demo verification — Week 1 playtest checklist (crash → scout → boss → claim home) | Pass/fail per beat; stability 2–5 min | SESSION_LOG or [DAY5_PLAYTEST_SIGNOFF § T1 verification](tasks/DAY5_PLAYTEST_SIGNOFF.md#t1-verification-current_task_list--week-1-playtest-loop) |
| 3 | Pre-demo checklist | [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) steps 1–8 (open DemoMap, PCG, PIE, pie_test_runner, Saved/pie_test_results.json) | Level open, character/Enhanced Input/GAS, chosen moment playable, stability 2–5 min | SESSION_LOG or VERTICAL_SLICE_CHECKLIST §3 |

**One or two sessions:** Run (1) and (2) can be done in one PIE session (tutorial then Week 1 playtest) or in two; (3) can be run before or after as the technical pre-demo check. Recommended: §3 first (level + PIE + pie_test_runner), then tutorial loop, then Week 1 playtest; document outcomes in the locations above.

---

<a id="pre-demo-verification-entry-point"></a>
## Pre-demo verification (entry point)

**§3 and CONSOLE_COMMANDS from one doc:** This document is the single entry point for pre-demo verification: it links (1) the step-by-step run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) and (2) the command reference → this document (Commands and Key PIE-test usage below). For MVP tutorial step 5 (gather), **List 6 scope** (what "collect wood, mine ore, pick flowers" means) is in [MVP_TUTORIAL_PLAN List 6 scope](workflow/MVP_TUTORIAL_PLAN.md#list-6-scope-gather-loop-wood-ore-flowers); verification steps are in **Tutorial (List 6) verification** below. For MVP tutorial steps 6–7 (lunch + dinner), **List 7 scope** (what "have lunch" and "have dinner" mean; day-only; named commands hw.Meal.Lunch, hw.Meal.Dinner) is in [MVP_TUTORIAL_PLAN List 7 scope](workflow/MVP_TUTORIAL_PLAN.md#list-7-scope-lunch--dinner); verification steps are in **Tutorial (List 7) verification** below. For MVP tutorial step 8 (go to bed), **List 8 scope** (what "go to bed" means: sleep trigger → transition to night, astral ready) is in [MVP_TUTORIAL_PLAN List 8 scope](workflow/MVP_TUTORIAL_PLAN.md#list-8-scope-go-to-bed); verification steps are in **Tutorial (List 8) verification** below. For MVP tutorial steps 9–12 (spectral self, combat encampment, beat boss, night ends), **List 9 scope** is in [MVP_TUTORIAL_PLAN List 9 scope](workflow/MVP_TUTORIAL_PLAN.md#list-9-scope-spectral-self-combat-boss-night-ends); verification steps are in **Tutorial (List 9) verification** below. For MVP tutorial step 13 (wake up — family taken), **List 10 scope** (what "wake up" means: dawn + player at spawn) is in [MVP_TUTORIAL_PLAN List 10 scope](workflow/MVP_TUTORIAL_PLAN.md#list-10-scope-wake-up--family-taken); verification steps are in **Tutorial (List 10) verification** below. Open this doc for the run sequence and commands; use Tutorial (List N) paragraphs for each step.

**How to run the pre-demo checklist:** Use a single sequence before recording or showing the slice.

1. **Step-by-step run sequence** — Follow [Vertical Slice Checklist §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing) (Pre-demo checklist). §3 gives the ordered steps: open DemoMap, ensure PCG generated, start PIE, run `pie_test_runner.py`, inspect `Saved/pie_test_results.json`, and optional stability check.
2. **Console commands** — The `hw.*` commands you need during that run (time-of-day, save/load, place, harvest, conversion, etc.) are listed in this document: see [Commands (hw.*)](#commands-hw) and [Key PIE-test usage](#key-pie-test-usage).

**Week 1 playtest checklist (crash → scout → boss → claim home):** Per [VISION.md](workflow/VISION.md) § Theme and prototype, the Week 1 playtest goal is *Survive 3 missions: crash → scout → boss → claim home.* Use the same entry point as above: (1) **Run sequence** → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); (2) **Commands** → this document. How to verify each beat: **(1) Crash-land / start** — Open DemoMap, ensure PCG generated, start PIE; character spawns at start, on ground. §3 step-by-step sequence + `pie_test_runner.py` → `Saved/pie_test_results.json` (Level, Character, PCG). **(2) Scout biomes** — In PIE, move through the level and/or run **`hw.Planetoid.ZoneInfo`** and **`hw.Planetoid.ZoneAlignment`** (Corrupted / Neutral / Positive) to confirm zone/biome read; explore or use commands to verify biome variety. **(3) Fight boss** — Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`** (night), confirm night encounter (Wave 1 in HUD); run **`hw.GrantBossReward`** [*amount*] and confirm Output Log / **`hw.Goods`** or HUD shows Wood. **(4) Claim home** — In PIE, use key **P** (Place) or **`hw.PlaceWall`** to place a build order at cursor; confirm building spawns (Moment = claim homestead). §3 Moment check and placement API in `pie_test_runner` cover this. If an existing PIE path already covers the full flow, document it here; otherwise the intended flow for future implementation is: run §3 → use commands above per beat.

**Tutorial (List 2) verification (morning at homestead):** To verify "wake up in homestead" (MVP tutorial List 2): run the §3 sequence above; in PIE on DemoMap (or Homestead) confirm time-of-day is morning — HUD shows "Phase: Day" or run `hw.TimeOfDay.Phase` (no arg) and confirm 0 in Output Log; confirm player spawns at homestead (viewport or log). **In-world wake at bed (List 56):** After night (Phase 2), interact (E) at bed or overlap bed trigger advances to Dawn; see **Tutorial (List 8) verification** (bed placement) and **Tutorial (List 10) verification** (wake steps). **Automated:** `pie_test_runner.py` includes a **DemoMap morning + spawn** check when the level is DemoMap (or Homestead): it asserts phase is Day (0) and the player pawn exists and is on ground; results appear in `Saved/pie_test_results.json`. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 2 scope.

**Tutorial (List 3) verification (family at breakfast):** "Family present at table" is satisfied when **actors in the level have the tag `Family`**. No separate breakfast table actor is required. **Restore/buff hook:** "Have breakfast" (hw.Meal.Breakfast or hw.ConsumeMealRestore) restores Health, sets the day buff, and contributes to love; the HUD shows "Restored today", "Love", and at night "Day buff: active" when the buff was earned. To verify: (1) Place one or more actors in the level and add the **Family** tag (Details → Actor → Tags → add `Family`). (2) Start PIE, ensure phase is Day (morning). (3) Run **`hw.Meal.Breakfast`** or use **in-world breakfast** (see below). (4) Confirm HUD shows "Restored today" ≥ 1, **"Meals with family: 1"** (or higher) if Family-tagged actors exist, and Love increased when family present. (5) Set `hw.TimeOfDay.Phase 2` (night) and confirm HUD shows "Day buff: active". If no Family-tagged actors exist, the meal still restores Health and sets day buff, but "Meals with family" stays 0. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 3 scope. **In-world breakfast (List 57 T2):** (1) **Interact:** Face an actor with tag **Breakfast** (e.g. **BP_MealTrigger_Breakfast**) and press **E** (Interact). Same effect as **hw.Meal.Breakfast** — restores Health, sets day buff, meals today. (2) **Overlap (optional):** If the actor has **HomeWorldMealTriggerComponent** (MealType = Breakfast), walking into the trigger volume also triggers breakfast. **Placement:** Create with **create_bp_meal_trigger_breakfast.py**; place one on DemoMap with **place_meal_triggers.py** (reads **breakfast_position** in `Content/Python/demo_map_config.json`). Add the overlap component via Editor (Add Component → HomeWorld Meal Trigger Component) or MCP `add_component_to_blueprint` (blueprint_name=BP_MealTrigger_Breakfast, component_type=HomeWorldMealTriggerComponent, component_name=MealTrigger).

**Tutorial (List 4) verification (love task with partner):** "Complete one love task with partner" (MVP tutorial step 3) is satisfied when the player completes the one love task — either by running **`hw.LoveTask.Complete`** in PIE or by **in-world interact** with the partner actor. **Partner identification (in-level):** The partner is the actor with family role **Partner** (`UHomeWorldFamilySubsystem::SetRoleForIndex` / `GetRoleForIndex`) or with actor tag **Partner**; in-world interact (E) targets that actor (List 58 T3). See [MVP_TUTORIAL_PLAN List 4 scope](workflow/MVP_TUTORIAL_PLAN.md#list-4-scope-love-task-with-partner) for full identification options (role, tag, first Family-tagged). The command adds Love +1 and increments LoveTasksCompletedToday; the HUD shows "Love: N" and the counter resets at dawn. **Console verification:** (1) Start PIE on DemoMap (phase Day). (2) Run **`hw.LoveTask.Complete`**. (3) Confirm HUD shows "Love: N" increased and Output Log shows "love tasks today: 1" (or higher if run again). (4) Optional: set night then dawn to confirm counter reset. **In-world verification (List 58 T3):** (1) Place an actor in DemoMap and add the **Partner** tag (Details → Actor → Tags → add `Partner`). (2) Start PIE on DemoMap (phase Day). (3) Face the partner actor and press **E** (Interact). (4) Confirm HUD "Love: N" increased and Output Log "Love task (interact with partner) — one love task done" and "love tasks today: 1". **Placement:** Create with **create_bp_partner_placeholder.py**; place one instance with **place_partner.py** (open DemoMap, run via Tools → Execute Python Script or MCP `execute_python_script("place_partner.py")`). Position is in `Content/Python/demo_map_config.json` **partner_position** (default near meal triggers; List 58 T4).

**Tutorial (List 5) verification (play game with child):** "Play one game with child" (MVP tutorial step 4) is satisfied when the player completes one game with child — either by running **`hw.GameWithChild.Complete`** in PIE or by **in-world interact** with the child actor. **Child identification (in-level):** The child is the actor with family role **Child** (`UHomeWorldFamilySubsystem::SetRoleForIndex` / `GetRoleForIndex`) or with actor tag **Child**; in-world interact (E) targets the actor with tag **Child** (List 59 T2/T3). The command adds Love +1 and increments GamesWithChildToday; the counter resets at dawn. **Console verification:** (1) Start PIE on DemoMap (phase Day). (2) Run **`hw.GameWithChild.Complete`**. (3) Confirm HUD shows "Love: N" increased and Output Log shows "games with child today: 1". **In-world verification (List 59 T3):** (1) Place an actor in DemoMap and add the **Child** tag (Details → Actor → Tags → add `Child`). (2) Start PIE on DemoMap (phase Day). (3) Face the child actor and press **E** (Interact). (4) Confirm HUD "Games with child: N" increased and Output Log "Game with child (interact with child) — one game with child done". **Placement:** Create with **create_bp_child_placeholder.py**; place one instance with **place_child.py** (open DemoMap, run via Tools → Execute Python Script or MCP `execute_python_script("place_child.py")`). Position is in `Content/Python/demo_map_config.json` **child_position** (default near partner; List 59 T4). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 5 scope.

**Tutorial (List 6) verification (gather — wood, ore, flowers):** "Collect wood, mine ore, pick flowers" (MVP tutorial step 5) is satisfied when the player harvests each type (or uses console stubs). **HUD:** The HUD shows **Physical: N** (combined total of all physical goods). To verify that you have collected wood, ore, and flowers: (1) use the harvest lines in Output Log (**"Harvest succeeded (Physical) - Wood +N"**, **Ore +N**, **Flowers +N**) for each type, and (2) run **`hw.Goods`** — it logs the physical total and, when you have any Wood/Ore/Flowers, a second line **Wood: N, Ore: N, Flowers: N** so you can confirm all three. **Wood:** Face **BP_HarvestableTree** (or any **AHomeWorldResourcePile** with ResourceType Wood), press **E**. Confirm Output Log **"Harvest succeeded (Physical) - Wood +N"** and HUD Physical count. If DemoMap has no trees, run **place_resource_nodes.py** (reads **demo_map_config.json** `resource_node_positions`). **Ore:** Face **BP_HarvestableOre** (ResourceType Ore), press **E**, or run **`hw.Gather.Ore`** [*amount*] in PIE to add Ore to inventory (default 10). Create ore Blueprint with **create_bp_harvestable_ore.py**; place in level via **place_resource_nodes.py** (uses **resource_node_ore_positions** in demo_map_config.json). **Flowers:** Face **BP_HarvestableFlower** (ResourceType Flowers), press **E**, or run **`hw.Gather.Flowers`** [*amount*] in PIE to add Flowers to inventory (default 5). Create flower Blueprint with **create_bp_harvestable_flower.py**; place in level via **place_resource_nodes.py** (uses **resource_node_flower_positions** in demo_map_config.json). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 6 scope.

**Tutorial (List 7) verification (lunch + dinner):** "Have lunch" (step 6) and "have dinner" (step 7) are available **during the day** only. Time-of-day phases are **0** = Day, **1** = Dusk, **2** = Night, **3** = Dawn; there is no midday/evening subdivision for List 7 — run **`hw.Meal.Lunch`** and **`hw.Meal.Dinner`** whenever the phase is Day (or Dawn). To verify steps 6–7: (1) Start PIE on DemoMap and ensure phase is Day (**`hw.TimeOfDay.Phase 0`** if needed). (2) **Console:** Run **`hw.Meal.Breakfast`**, then **`hw.Meal.Lunch`**, then **`hw.Meal.Dinner`**. (3) Confirm HUD **"Restored today"** ≥ 3 and **"Meals with family: N"** (if Family-tagged actors exist). All three commands use the same ConsumeMealRestore logic (Health, day buff, love). **In-world lunch (List 57 T3):** (1) **Interact:** Face an actor with tag **Lunch** (e.g. **BP_MealTrigger_Lunch**) and press **E** (Interact). Same effect as **hw.Meal.Lunch**. (2) **Overlap (optional):** If the actor has **HomeWorldMealTriggerComponent** (MealType = Lunch), walking into the trigger volume also triggers lunch. **Placement:** Create with **create_bp_meal_trigger_lunch.py**; place on DemoMap with **place_meal_triggers.py** (reads **lunch_position** in `Content/Python/demo_map_config.json`). **In-world dinner (List 57 T4):** (1) **Interact:** Face an actor with tag **Dinner** (e.g. **BP_MealTrigger_Dinner**) and press **E** (Interact). Same effect as **hw.Meal.Dinner**. (2) **Overlap (optional):** If the actor has **HomeWorldMealTriggerComponent** (MealType = Dinner), walking into the trigger volume also triggers dinner. **Placement:** Create with **create_bp_meal_trigger_dinner.py**; place on DemoMap with **place_meal_triggers.py** (reads **dinner_position** in `Content/Python/demo_map_config.json`). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 7 scope.

**Tutorial (List 8) verification (go to bed):** "Go to bed" (MVP tutorial step 8) is satisfied when time-of-day becomes **Night** (Phase 2) so the player "wakes" in astral / night phase. **Transition to night — astral ready:** After the sleep trigger, `GetCurrentPhase()` returns Night (Phase 2), the HUD shows **"Phase: Night"**, Defend phase is active, and astral abilities (SpiritBurst, SpiritShield) and the night encounter (Wave 1 placeholder spawn) are available. To verify: (1) Start PIE on DemoMap (any phase). (2) Run **`hw.GoToBed`** or **`hw.Sleep`** (or **`hw.TimeOfDay.Phase 2`**). (3) Confirm HUD shows **"Phase: Night"**, then run **`hw.SpiritBurst`** or **`hw.SpiritShield`** (if abilities granted) and confirm Output Log "activated"; confirm night encounter Wave 1 spawn in Log ("Night encounter Wave 1 — spawned placeholder"). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 8 scope. **Bed actor (List 56):** In-world bed is **BP_Bed** in `/Game/HomeWorld/Building/`. Create with **create_bp_bed.py**; place one instance in DemoMap with **place_bed.py** (open DemoMap, then run via Tools → Execute Python Script or MCP `execute_python_script("place_bed.py")`). Position is in `Content/Python/demo_map_config.json` **bed_position** (default near homestead). **In-world go-to-bed (T2):** (1) **Interact:** Face the bed and press **E** (Interact). Same effect as **hw.GoToBed** — phase set to Night; HUD shows "Phase: Night". (2) **Overlap (optional):** If **BP_Bed** has the **HomeWorldGoToBedTriggerComponent** (add in Editor: Add Component → HomeWorld Go To Bed Trigger Component, or MCP `add_component_to_blueprint` with blueprint_name BP_Bed, component_type HomeWorldGoToBedTriggerComponent), walking into the bed trigger also sets phase to Night. **Bed interact (List 56 T4):** Satisfied by T2/T3. GA_Interact (E) uses the character’s forward trace; any actor with tag **Bed** (BP_Bed default from create_bp_bed.py) triggers go-to-bed (day) or wake (night). Optional overlap via GoToBedTriggerComponent. **In-world wake (List 56 T3):** When at **night** (Phase 2), interact (E at bed) or overlap (walk into bed trigger) advances to **Dawn** (Phase 3). Same effect as **hw.Wake**. Verify: HUD shows **"Phase: Dawn"**; or run **`hw.TimeOfDay.Phase`** (no arg) and confirm Output Log shows 3.

**Tutorial (List 9) verification (spectral self / astral out — step 9):** "Spectral self — go out into the world" (MVP tutorial step 9) is satisfied when the player is at **night (Phase 2)** with **astral abilities** and the **night encounter** active — same character, not a separate astral actor. To verify step 9: (1) Start PIE on DemoMap. (2) Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`**. (3) Confirm HUD shows **"Phase: Night"**. (4) Run **`hw.SpiritBurst`** or **`hw.SpiritShield`** and confirm Output Log shows ability activation. (5) Confirm night encounter Wave 1 in Output Log ("Night encounter Wave 1 — spawned placeholder"). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 9 scope.

**Tutorial (List 9) verification (combat with encampment — step 10):** "Combat with an encampment" (MVP tutorial step 10) is satisfied when the **night encounter** is active: waves of placeholder foes spawn at night (Phase 2). "Encampment" = night encounter waves (Wave 1, 2, 3). To verify step 10: (1) Start PIE on DemoMap. (2) Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`**. (3) Confirm HUD shows **"Phase: Night"** and **"Wave 1"** (or higher). (4) Confirm Output Log shows "Night encounter Wave 1 — spawned placeholder" (and Wave 2/3 when triggered). (5) Optional: **`hw.SpiritBurst`** / **`hw.SpiritShield`** for combat; **`hw.CombatStubs`** to log Defend combat mode. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 9 scope.

**Tutorial (List 9) verification (beat the boss — step 11):** "Beat the boss" (MVP tutorial step 11) is satisfied when the player has defeated or converted the **key-point boss placeholder** (spawned at night when GameMode **KeyPointBossSpawnDistance** > 0), or for PIE verification when the reward is granted. To verify step 11: (1) Start PIE on DemoMap. (2) Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`** (night). (3) If a key-point boss placeholder is present, defeat or convert it; otherwise skip. (4) Run **`hw.GrantBossReward`** [*amount*] in the console (default amount 100). (5) Confirm **HUD shows "Boss reward: +N Wood"** (yellow toast for 4s), Output Log shows "hw.GrantBossReward granted Wood +N", and **`hw.Goods`** or HUD Physical count shows Wood. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 9 scope.

**Tutorial (List 9) verification (night ends — step 12):** "Night ends" (MVP tutorial step 12) is satisfied when the night phase ends and the player returns to dawn (Phase 3 → respawn at start). The console command **`hw.AstralDeath`** simulates astral death: it advances time to **Dawn** and respawns the player at the level start. To verify step 12: (1) Start PIE on DemoMap. (2) Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`** (night). (3) Confirm HUD shows **"Phase: Night"**. (4) Run **`hw.AstralDeath`** in the console. (5) Confirm HUD shows **"Phase: Dawn"** (or Day) and the player has respawned at start; Output Log may show advance-to-dawn/respawn. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 9 scope. **Automated:** `pie_test_runner` has `check_astral_death` for this flow.

**Tutorial (List 10) verification (wake up — step 13):** "Wake up" (MVP tutorial step 13) is satisfied when, **after night ends**, the game is in **Dawn (Phase 3)** (or **Day (Phase 0)** for "morning") and the player has **respawned at the level start** (homestead). **Wake trigger (List 56 T3):** (1) **In-world:** At night, interact (E) at bed or overlap bed trigger → phase advances to Dawn; no respawn. (2) **Console:** **`hw.Wake`** — advance to Dawn when current phase is Night (no respawn). (3) **Full wake (respawn):** **`hw.AstralDeath`** — dawn + respawn at start = "wake up" for tutorial. To verify step 13: (1) Start PIE on DemoMap. (2) Run **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`** (night). (3) Confirm HUD shows **"Phase: Night"**. (4) Run **`hw.Wake`** (Dawn only) or **`hw.AstralDeath`** (Dawn + respawn). (5) Confirm HUD shows **"Phase: Dawn"** (or Day); after **hw.AstralDeath**, player at start. **Verify morning phase:** Run **`hw.TimeOfDay.Phase`** (no arg) — 3 = Dawn, 0 = Day (morning). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 10 scope. **Automated:** `pie_test_runner` has `check_astral_death` for night → dawn + respawn.

**Tutorial (List 10) verification (family taken — tutorial end):** "Family taken" is the **tutorial end trigger** and **inciting incident** for Act 1. To verify tutorial end in PIE: (1) Start PIE on DemoMap. (2) Run **`hw.TutorialEnd`** or **`hw.FamilyTaken`**. (3) Confirm Output Log shows "Family taken — tutorial complete; inciting incident" and "bTutorialComplete set". (4) The local player's PlayerState now has `GetTutorialComplete() == true` (readable by Blueprint or C++ for Act 1 handoff). See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 10 scope. **Automated:** `pie_test_runner.py` includes a **Tutorial complete (hw.TutorialEnd)** check: it runs `hw.TutorialEnd` in PIE and asserts `GetTutorialComplete()` is true (or logs a verify-in-Output-Log note if the flag is not readable from Python); results appear in `Saved/pie_test_results.json`.

**After tutorial (Act 1 handoff):** Once the tutorial ends (family taken), the **next phase** is **Act 1 (lone wanderer)** — the player sets out to rescue the family. To verify in PIE: run **`hw.TutorialEnd`** or **`hw.FamilyTaken`**, then confirm `GetTutorialComplete() == true` on the local PlayerState. **Act 1 objective (when implemented):** "Find your family." **Next step (when implemented):** Load the Act 1 map (e.g. level transition or console command **`hw.Act1.Start`** — not yet implemented; doc only). Future implementation may use the flag to show this objective in HUD/UI or gate Act 1 content. See [VISION.md](workflow/VISION.md) § Campaign summary and [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 10 scope (Handoff to Act 1).

**Post-tutorial and Week 1 playtest — single reference (PIE sequence):**

- **After hw.TutorialEnd:** Run **`hw.TutorialEnd`** or **`hw.FamilyTaken`**; confirm Output Log "Family taken — tutorial complete; inciting incident" and PlayerState `GetTutorialComplete() == true`. Act 1 handoff: objective "Find your family" (when implemented); **`hw.Act1.Start`** (future) for Act 1 map. Same entry point as above: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this document.
- **Week 1 playtest (crash → scout → boss → claim home):** (1) **Crash/start** — Open DemoMap, PCG generated, start PIE; run §3 steps + **`pie_test_runner.py`** → `Saved/pie_test_results.json`. (2) **Scout** — **`hw.Planetoid.ZoneInfo`**, **`hw.Planetoid.ZoneAlignment`** (Corrupted/Neutral/Positive); explore level. (3) **Boss** — **`hw.GoToBed`** or **`hw.TimeOfDay.Phase 2`** (night), confirm Wave 1 in HUD; **`hw.GrantBossReward`** [*amount*]; **`hw.Goods`** or HUD for Wood. (4) **Claim home** — Key **P** (Place) or **`hw.PlaceWall`** at cursor; confirm building spawns. Full detail for each beat is in the **Week 1 playtest checklist** paragraph above and in [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing).

**Agentic building (List 60) verification:** Full agentic building (MVP full scope List 60) is "family agent claims and completes one build order." **Console commands:** (1) **`hw.PlaceWall`** — place a build-order actor (e.g. BP_BuildOrder_Wall) at the player cursor; requires PIE and build-order setup (e.g. **place_build_order_wall.py** / **demo_map_config** `build_order_wall_position`). (2) **`hw.CompleteBuildOrder`** — complete the nearest incomplete build order (hologram → built); use in PIE to test the completion path without a family agent. (3) **`hw.SimulateBuildOrderActivation`** — simulate SO_WallBuilder activation on the nearest incomplete build order (logs activation and calls CompleteBuildOrder); use so activation is triggerable and observable in PIE. **How to verify "family agent completes one build order" in PIE:** (a) **With family agents (State Tree / Mass):** Ensure DemoMap has at least one incomplete build order and family agents using the BUILD flow; run PIE, observe an agent move to the build order and the build complete (or follow [AGENTIC_BUILDING.md](tasks/AGENTIC_BUILDING.md) and [DAY10_AGENTIC_BUILDING.md](tasks/DAY10_AGENTIC_BUILDING.md) for manual State Tree BUILD branch if needed). (b) **Commands-only verification:** Run **`hw.PlaceWall`** to place an incomplete build order, then **`hw.SimulateBuildOrderActivation`** (or **`hw.CompleteBuildOrder`**) to complete it; confirm in Output Log and in-world that the build order changes from incomplete to complete. Same entry point as above: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this doc. See [MVP_TUTORIAL_PLAN List 60 (agentic building)](workflow/MVP_TUTORIAL_PLAN.md#list-60-mvp-full-scope-agentic-building).

**Astral-by-day (List 61) verification:** Enter astral during the day (MVP full scope List 61) is a stub: from Day or Dusk the player can enter astral (phase → Night); return (astral death or F8) restores **Day** (not Dawn) per [ASTRAL_DEATH_AND_DAY_SAFETY.md](tasks/ASTRAL_DEATH_AND_DAY_SAFETY.md) §3. **Console commands:** **`hw.EnterAstral`** or **`hw.AstralByDay`** (alias). **How to verify "enter astral during day" in PIE:** (1) Start PIE on DemoMap. (2) Set day: run **`hw.TimeOfDay.Phase 0`** (or 1 for Dusk). (3) Run **`hw.EnterAstral`** or **`hw.AstralByDay`**. (4) Confirm HUD shows **"Phase: Night"** and Output Log shows "Enter astral during day". (5) Return: run **`hw.AstralDeath`** (or press **F8** if bound). (6) Confirm HUD shows **"Phase: Day"** (not Dawn) and Output Log shows "Return from astral-by-day — phase restored to Day." Same entry point as above: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this doc. See [MVP_TUTORIAL_PLAN List 61 (astral-by-day)](workflow/MVP_TUTORIAL_PLAN.md#list-61-mvp-full-scope-astral-by-day).

**Defend-at-night (List 62) verification:** When it is night (Phase 2), family (actors with tag **Family**) teleport to **DefendPosition**-tagged actors; at dawn they return to **GatherPosition**-tagged actors (or GatherReturnOffset if none). **Setup (Editor, DemoMap open):** Run **place_defend_gather_positions.py** to place one DefendPosition and one GatherPosition actor; run **place_partner.py** or **place_child.py** so at least one actor has the **Family** tag. **PIE verification:** (1) Start PIE on DemoMap. (2) Run **`hw.TimeOfDay.Phase 2`** (night). (3) Output Log shows "HomeWorld: T3 moved N family actor(s) to DefendPosition (teleport)." and family actor(s) move to the DefendPosition location. (4) Run **`hw.Defend.Status`** to log phase, DefendActive, DefendPosition count, Family count. (5) Run **`hw.TimeOfDay.Phase 0`** (day) or **`hw.TimeOfDay.Phase 3`** (dawn). (6) Output Log shows "Defend phase end (dawn)." and "T3 return from Defend at dawn — moved N family actor(s) to GatherPosition (teleport)." Same entry point as above: run sequence → [VERTICAL_SLICE_CHECKLIST §3](workflow/VERTICAL_SLICE_CHECKLIST.md#3-pre-demo-checklist-before-recording-or-showing); commands → this doc. See [DAY12_ROLE_PROTECTOR.md](tasks/DAY12_ROLE_PROTECTOR.md) §2.

---

## Commands (hw.*)

| Command | Description |
|--------|-------------|
| **hw.Save** | Save game (roles, spirit roster, phase, etc.) to default slot. Use in PIE for SaveGame verification (e.g. DAY15 §4). |
| **hw.Load** | Load game from default slot. Use in PIE for SaveGame verification. |
| **hw.Roles** | Log current family roles (index → role: Gatherer, Protector, Healer, Child, Partner). Use after **hw.Save** then **hw.Load** to verify role persistence. See [DAY15_ROLE_PERSISTENCE.md](tasks/DAY15_ROLE_PERSISTENCE.md) §4. |
| **hw.ReportDeath** | Report player death and add player to spirit roster (T5 / Day 21). Verifiable via `pie_test_runner` check_report_death. |
| **hw.Spirits** | List spirit roster: count and spirit IDs. Run **hw.ReportDeath** then **hw.Spirits** to verify death-to-spirit (T5 / Day 21 / List 70). |
| **hw.GrantBossReward** [*amount*] | Grant boss reward (Wood). Default amount 100; e.g. `hw.GrantBossReward 50`. HUD shows "Boss reward: +N Wood" toast for 4s; use in PIE for Day 25 verification. |
| **hw.PlaceWall** | Place build-order actor (e.g. BP_BuildOrder_Wall) at cursor. Requires PIE; run build-order setup script first. |
| **hw.AstralDeath** | Simulate astral death: advance time to dawn and respawn player at start. When returning from **astral-by-day** (hw.EnterAstral), restores Day instead of Dawn. Use in PIE to test astral-return-on-death (ASTRAL_DEATH_AND_DAY_SAFETY.md). |
| **hw.EnterAstral** | List 61: Enter astral during the day (stub). From Day or Dusk, sets phase to Night; return with hw.AstralDeath (or F8) restores Day. See ASTRAL_DEATH_AND_DAY_SAFETY.md §3. |
| **hw.AstralByDay** | Alias for hw.EnterAstral (enter astral during day, List 61). |
| **hw.CompleteBuildOrder** | Complete the nearest incomplete build order (e.g. wall hologram). Use in PIE to test agentic building (DAY10_AGENTIC_BUILDING.md). |
| **hw.SimulateBuildOrderActivation** | Simulate SO_WallBuilder activation on nearest incomplete build order (log + CompleteBuildOrder). Use in PIE so activation is triggerable and observable. |
| **hw.SpiritualPower** | Log spiritual power and spiritual artefacts counts (PlayerState). Set `hw.TimeOfDay.Phase 2`, overlap spiritual collectible, then run to verify. |
| **hw.SpendSpiritualPower** [*N*] | Spend N spiritual power (e.g. `hw.SpendSpiritualPower 5`). Deducts from SpiritualPowerCollected; logs stub "upgrade unlocked". Use in PIE to verify spend path. |
| **hw.Goods** | Log physical (day) and spiritual (night) goods: physical total from InventorySubsystem, spiritual power and spiritual artefacts from PlayerState. When you have Wood, Ore, or Flowers, a second line logs **Wood: N, Ore: N, Flowers: N** for List 6 gather verification. |
| **hw.SpiritBurst** | Trigger GA_SpiritBurst ability. Only succeeds when `hw.TimeOfDay.Phase 2` (night). Run create_ga_spirit_burst.py to add ability to character. |
| **hw.SpiritShield** | Trigger GA_SpiritShield ability. Only succeeds when `hw.TimeOfDay.Phase 2` (night). Run create_ga_spirit_shield.py to add ability and bind key. |
| **hw.RestoreMeal** | Day restoration: consume meal (day only). Restores Health +25 and sets day buff for next night. At night HUD shows "Day buff: active" if set (DAY_RESTORATION_LOOP.md). |
| **hw.Meal.Breakfast** | MVP tutorial step 2: have breakfast (day only). Same logic as hw.RestoreMeal — restores Health, sets day buff, meals today, love; counts Family-tagged actors. Use in morning for List 3 verification. |
| **hw.Meal.Lunch** | MVP tutorial step 6: have lunch (day only). Same logic as hw.RestoreMeal — restores Health, sets day buff, meals today, love; counts Family-tagged actors. Use for List 7 verification (run during day; HUD Restored today ≥ 2 after breakfast + lunch). |
| **hw.Meal.Dinner** | MVP tutorial step 7: have dinner (day only). Same logic as hw.RestoreMeal — restores Health, sets day buff, meals today, love; counts Family-tagged actors. Use for List 7 verification (run during day; HUD Restored today ≥ 3 after breakfast + lunch + dinner). |
| **hw.LoveTask.Complete** | MVP tutorial step 3: complete one love task with partner (PIE verification). Adds Love +1 and increments LoveTasksCompletedToday; HUD shows "Love: N". Counter resets at dawn. Use for List 4 verification. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 4 scope. |
| **hw.GameWithChild.Complete** | MVP tutorial step 4: complete one game with child (PIE verification). Adds Love +1 and increments GamesWithChildToday; counter resets at dawn. Use for List 5 verification. See [MVP_TUTORIAL_PLAN](workflow/MVP_TUTORIAL_PLAN.md) List 5 scope. |
| **hw.Gather.Ore** [*amount*] | Add Ore to inventory (default 10). Stub for MVP tutorial List 6 step 5. Use in PIE to verify "mine some ore" or harvest from BP_HarvestableOre. |
| **hw.Gather.Flowers** [*amount*] | Add Flowers to inventory (default 5). Stub for MVP tutorial List 6 step 5. Use in PIE to verify "pick some flowers" or harvest from BP_HarvestableFlower. |
| **hw.TestGrantSpiritualCollect** | Test-only: grant one spiritual collect using same formula as SpiritualCollectible (base + day buff + love). **Night only.** For pie_test_runner day-buff-bonus and love-bonus checks. |
| **hw.Conversion.Test** | Trigger conversion hook (ReportFoeConverted) for testing. Logs conversion and increments ConvertedFoesThisNight (CONVERSION_NOT_KILL.md). |
| **hw.CombatStubs** | Log DefendCombatMode (Ranged \| GroundAOE), PlanetoidCombatStyle (Combo \| SingleTarget), and ComboHitCount to Output Log. Use in PIE to verify combat stubs. See [DEFEND_COMBAT.md](tasks/DEFEND_COMBAT.md), [PLANETOID_COMBAT.md](tasks/PLANETOID_COMBAT.md). |
| **hw.Defend.Status** | Act 2 prep: log Defend phase status (phase 0–3, DefendActive, DefendPosition count, Family count, family-moved-this-night). Use in PIE after `hw.TimeOfDay.Phase 2` to verify Defend at home. See [DAY12_ROLE_PROTECTOR.md](tasks/DAY12_ROLE_PROTECTOR.md). |
| **hw.Planetoid.Complete** | Set planetoid-complete flag on GameMode (bPlanetoidComplete). Use in PIE to test "complete planetoid → travel to next" flow. Logs "planetoid complete"; pie_test_runner can verify flag. See [PLANETOID_HOMESTEAD.md](tasks/PLANETOID_HOMESTEAD.md) §5. |
| **hw.Planetoid.ZoneAlignment** [*Corrupted* \| *Neutral* \| *Positive*] | Get or set current zone alignment (fight / harvest / empower). No arg = log current; with arg = set. Read at runtime via GameMode GetCurrentZoneAlignment. See [PLANETOID_BIOMES.md](PLANETOID_BIOMES.md) §3. |
| **hw.Planetoid.ZoneInfo** | Log current zone alignment and biome from GameMode. Use in PIE to verify alignment/biome read at runtime (fight/harvest/empower branching). See [PLANETOID_BIOMES.md](PLANETOID_BIOMES.md) §3. |
| **hw.SinVirtue.Pride** | Log current Pride axis stub value (e.g. 0). Design only; no gameplay implementation. Use in PIE to read one sin/virtue axis. See [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) §2. |
| **hw.SinVirtue.Greed** | Log current Greed axis stub value (e.g. 0). Design only; no gameplay implementation. Use in PIE to read one sin/virtue axis. See [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) §2. |
| **hw.SinVirtue.Wrath** | Log current Wrath axis stub value (e.g. 0). Design only; no gameplay implementation. Use in PIE to read one sin/virtue axis. See [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) §2. |
| **hw.SinVirtue.Envy** | Log current Envy axis stub value (e.g. 0). Design only; no gameplay implementation. Use in PIE to read one sin/virtue axis. See [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) §2. |
| **hw.TutorialEnd** | MVP tutorial List 10: mark "family taken" / tutorial end. Sets bTutorialComplete on PlayerState; logs "Family taken — tutorial complete; inciting incident." Use in PIE to verify step 13 (tutorial end). See CONSOLE_COMMANDS § Tutorial (List 10) verification. |
| **hw.FamilyTaken** | Alias for hw.TutorialEnd: mark family taken / tutorial complete (inciting incident). MVP tutorial List 10 step 13. |
| **hw.Act1.Start** | *(Future; not yet implemented.)* Load Act 1 map / start lone-wanderer phase. Doc only; when implemented, use after **hw.TutorialEnd** to transition to Act 1. See § After tutorial (Act 1 handoff). |
| **hw.GoToBed** | Go to bed: set time-of-day to Night (Phase 2). Player "wakes" in astral / night phase. Use for MVP tutorial List 8 step 8 verification. Alternative: `hw.TimeOfDay.Phase 2`. |
| **hw.Sleep** | Alias for hw.GoToBed: set time-of-day to Night (Phase 2). MVP tutorial List 8 step 8. |
| **hw.Wake** | Wake: advance time-of-day to Dawn (Phase 3). Only has effect when current phase is Night. Use in PIE for List 56 T3 verification. In-world: interact (E) or overlap bed at night for same effect. |

---

## Console variables (cvars)

| Cvar | Default | Description |
|------|---------|-------------|
| **hw.TimeOfDay.Phase** | 0 | Override time-of-day phase: **0** = Day, **1** = Dusk, **2** = Night, **3** = Dawn. **-1** = use default (Day). Set in PIE with e.g. `hw.TimeOfDay.Phase 2` for night; used by Defend branch, spirit abilities, spiritual collectibles. |
| **hw.TimeOfDay.NightDurationSeconds** | 120 | Night phase duration in seconds for stub countdown ("Dawn in Ns"). Used when phase is set to Night. |

---

## Key PIE-test usage

- **TimeOfDay:** Use `hw.TimeOfDay.Phase 0` for day, `hw.TimeOfDay.Phase 2` for night (or **`hw.GoToBed`** / **`hw.Sleep`** for "go to bed" → Night). Many checks (Defend, spirit abilities, spiritual collectibles, day buff at night) require Phase 2. **pie_test_runner** includes a **Current TimeOfDay phase** check that reads and reports the current phase (0=Day, 1=Dusk, 2=Night, 3=Dawn) in `Saved/pie_test_results.json` for §3 verification without running `hw.TimeOfDay.Phase` (no arg) in the console.
- **Save/Load:** In PIE run `hw.Save` then `hw.Load`; run `hw.Roles` to verify role persistence. See § [SaveGame and role persistence (T2 / Day 15)](#savegame-and-role-persistence-t2--day-15). `pie_test_runner` includes `check_save_load_persistence` and related checks.
- **Death and boss reward:** `hw.ReportDeath` (spirit roster); **`hw.Spirits`** (list roster count and IDs after ReportDeath); `hw.GrantBossReward` or `hw.GrantBossReward 50`. Validated by `check_report_death` and `check_grant_boss_reward` in pie_test_runner.
- **Day restoration / breakfast / lunch / dinner:** `hw.RestoreMeal` or **`hw.Meal.Breakfast`** (day only; step 2); **`hw.Meal.Lunch`** (step 6, List 7); **`hw.Meal.Dinner`** (step 7, List 7). All use same ConsumeMealRestore logic; run during day; then set Phase 2 and use `hw.TestGrantSpiritualCollect` to verify day-buff bonus; `hw.SpiritualPower` or `hw.Goods` to confirm counters. HUD "Restored today" and "Meals with family" reflect multiple meals.
- **Conversion:** `hw.Conversion.Test` to trigger one conversion and see ConvertedFoesThisNight / role in Output Log.
- **Astral death:** Set `hw.TimeOfDay.Phase 2`, run `hw.AstralDeath`; expect dawn + respawn at start (pie_test_runner has check_astral_death).
- **Astral-by-day (List 61):** Run `hw.TimeOfDay.Phase 0` (day), then `hw.EnterAstral` or `hw.AstralByDay`; phase becomes Night. Run `hw.AstralDeath` (or F8); phase restores to Day (not Dawn). Output Log: "Enter astral during day" and "Return from astral-by-day — phase restored to Day." See ASTRAL_DEATH_AND_DAY_SAFETY.md §3.
- **Tutorial end (family taken):** Run **`hw.TutorialEnd`** or **`hw.FamilyTaken`** to mark tutorial complete; Output Log shows "Family taken — tutorial complete; inciting incident." PlayerState GetTutorialComplete() is true. MVP tutorial List 10 step 13.
- **Spirit abilities:** Set `hw.TimeOfDay.Phase 2`, then `hw.SpiritBurst` or `hw.SpiritShield` (abilities must be granted to character).
- **Night encounter (one spawn at Phase 2):** Set `hw.TimeOfDay.Phase 2` in PIE; Wave 1 spawns one placeholder (Cube) in front of the player. Output Log: `HomeWorld: Night encounter Wave 1 — spawned placeholder at ...`. HUD shows "Wave 1" and "Phase: Night". See [NIGHT_ENCOUNTER.md](tasks/NIGHT_ENCOUNTER.md) §2.1.
- **Combat stubs (DefendCombatMode, PlanetoidCombatStyle, ComboHitCount):** In PIE, run **`hw.CombatStubs`** to print current values to the Output Log (e.g. `DefendCombatMode=Ranged`, `PlanetoidCombatStyle=SingleTarget`, `ComboHitCount=0`). To change DefendCombatMode or PlanetoidCombatStyle, select the player's **PlayerState** in the World Outliner, then in the Details panel use **HomeWorld|Defend** and **HomeWorld|Planetoid**; run `hw.CombatStubs` again to confirm. See [DEFEND_COMBAT.md](tasks/DEFEND_COMBAT.md) and [PLANETOID_COMBAT.md](tasks/PLANETOID_COMBAT.md) "Testing in PIE".
- **Defend status (Act 2 prep):** In PIE, run **`hw.TimeOfDay.Phase 2`** (night), then **`hw.Defend.Status`** to log Defend phase active, DefendPosition actor count, Family actor count, and whether family were moved to Defend positions this night. Add **DefendPosition** and **Family** tags to level actors for T3 teleport. See [DAY12_ROLE_PROTECTOR.md](tasks/DAY12_ROLE_PROTECTOR.md).
- **Planetoid complete:** In PIE, run **`hw.Planetoid.Complete`** to set the GameMode's planetoid-complete flag. Output Log shows "planetoid complete (bPlanetoidComplete set)". Use to test the "complete → travel to next" flow; optional pie_test_runner check can verify the flag. See [PLANETOID_HOMESTEAD.md](tasks/PLANETOID_HOMESTEAD.md) §5.
- **Agentic building (List 60):** **`hw.PlaceWall`** (place build order at cursor), **`hw.CompleteBuildOrder`** (complete nearest incomplete build order), **`hw.SimulateBuildOrderActivation`** (simulate SO_WallBuilder activation). Use to verify "family agent completes one build order" in PIE (observe agent or use commands); see § Agentic building (List 60) verification.
- **Planetoid zone alignment (fight/harvest/empower):** In PIE, run **`hw.Planetoid.ZoneInfo`** to log current zone alignment and biome. Run **`hw.Planetoid.ZoneAlignment Corrupted`** (or **Neutral**, **Positive**) to set alignment. GameMode exposes GetCurrentZoneAlignment / GetCurrentZoneBiome for runtime branching. See [PLANETOID_BIOMES.md](PLANETOID_BIOMES.md) §3. To validate biome/alignment configs without PIE, run `test_biome_alignment_config.py` (see PLANETOID_BIOMES §3.3).
- **Sin/virtue (stub):** In PIE, run **`hw.SinVirtue.Pride`**, **`hw.SinVirtue.Greed`**, **`hw.SinVirtue.Wrath`**, or **`hw.SinVirtue.Envy`** to log the current axis stub value (e.g. `Pride: 0`, `Greed: 0`, `Wrath: 0`, `Envy: 0`) to the Output Log. Design only; see [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) §2.

**Running checks:** With Editor open and PIE running, execute `pie_test_runner.py` via MCP (`execute_python_script("pie_test_runner.py")`) or Tools → Execute Python Script; read `Saved/pie_test_results.json` for pass/fail and detail. See [VERTICAL_SLICE_CHECKLIST](workflow/VERTICAL_SLICE_CHECKLIST.md) §3 for pre-demo checklist.

---

## SaveGame and role persistence (T2 / Day 15)

**Canonical procedure:** [DAY15_ROLE_PERSISTENCE.md](tasks/DAY15_ROLE_PERSISTENCE.md) §4.

- **In-session:** In PIE run **`hw.Save`** then **`hw.Load`**; run **`hw.Roles`** to log current family roles. Confirm Output Log shows "Save completed to slot 'HomeWorldSave' (roles=N, spirits=M)" and "Load completed from slot 'HomeWorldSave' (roles=N, spirits=M)". Roles and spirit roster round-trip in the same session.
- **Across PIE restart:** Run **`hw.Save`** in PIE, stop PIE, start PIE, run **`hw.Load`**, then **`hw.Roles`**. Roles (and spirit roster) restore from slot; **hw.Roles** confirms the same index→role mapping. Full steps and expected log lines: [DAY15_ROLE_PERSISTENCE.md §4](tasks/DAY15_ROLE_PERSISTENCE.md#4-validation).
- **Automated:** **pie_test_runner** `check_save_load_persistence` (in-session Save/Load when PIE is running; result in `Saved/pie_test_results.json`). **verify_save_load_cross_restart.py** (hw.Save → stop PIE → start PIE → hw.Load; run via MCP or Tools → Execute Python Script; result in `Saved/save_load_cross_restart_result.json`; may exceed MCP timeout).

---

## How to test zone alignment in PIE

Use this sequence to validate zone alignment (fight / harvest / empower) and GameMode state without reading code. See [PLANETOID_BIOMES.md](PLANETOID_BIOMES.md) §3 for alignment behavior.

1. **Start PIE** on DemoMap (or any planetoid level).
2. **Log current state:** In the console (~), run **`hw.Planetoid.ZoneInfo`**. The Output Log shows current zone alignment and biome from the GameMode.
3. **Set alignment (optional arg):** Run **`hw.Planetoid.ZoneAlignment Corrupted`**, **`hw.Planetoid.ZoneAlignment Neutral`**, or **`hw.Planetoid.ZoneAlignment Positive`**. No argument only logs current alignment; with an argument the GameMode updates the current zone alignment.
4. **Confirm GameMode state:** Run **`hw.Planetoid.ZoneInfo`** again. The Output Log should show the alignment (and biome) you set. Code can read the same state via `GetCurrentZoneAlignment()` and `GetCurrentZoneBiome()` for fight / harvest / empower branching.

**Summary:** `hw.Planetoid.ZoneInfo` = read state; `hw.Planetoid.ZoneAlignment <Corrupted|Neutral|Positive>` = set alignment; `hw.Planetoid.ZoneInfo` again = confirm.

---

## Testing sin/virtue in PIE

Short guide so testers and agents can validate the sin/virtue system in PIE without re-researching. Ties to [VISION.md](workflow/VISION.md) § Moral system and [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md).

**How to run:** Start PIE (DemoMap or any playable level). Open the console (~ or `) and type **`hw.SinVirtue.Pride`**, **`hw.SinVirtue.Greed`**, **`hw.SinVirtue.Wrath`**, or **`hw.SinVirtue.Envy`** then Enter. No arguments. All four commands are cheat-only (require a play world).

**What output to expect:** In the Editor **Output Log**, you should see one line per command, for example:

- `LogTemp: HomeWorld: Pride: 0 (stub; sin/virtue axis -1..0..+1). See SIN_VIRTUE_SPECTRUM.md, CONSOLE_COMMANDS.md.`
- `LogTemp: HomeWorld: Greed: 0 (stub; sin/virtue axis -1..0..+1). See SIN_VIRTUE_SPECTRUM.md, CONSOLE_COMMANDS.md.`
- `LogTemp: HomeWorld: Wrath: 0 (stub; sin/virtue axis -1..0..+1). See SIN_VIRTUE_SPECTRUM.md, CONSOLE_COMMANDS.md.`
- `LogTemp: HomeWorld: Envy: 0 (stub; sin/virtue axis -1..0..+1). See SIN_VIRTUE_SPECTRUM.md, CONSOLE_COMMANDS.md.`

Values are currently stubs (0). When the moral system is implemented, they will reflect -1..0..+1 per axis.

**How it ties to the moral system:** [VISION.md](workflow/VISION.md) § Moral system and [SIN_VIRTUE_SPECTRUM.md](tasks/SIN_VIRTUE_SPECTRUM.md) define seven sin/virtue axes (Pride/Humility, Greed/Generosity, Wrath/Grace, Envy/Fulfillment, etc.). Each axis runs from **-1** (sin) to **0** (neutral) to **+1** (virtue). These console commands are the in-PIE way to read the current stub value for four axes (Pride, Greed, Wrath, Envy). The same four axes are shown on the HUD per [VERTICAL_SLICE_CHECKLIST](workflow/VERTICAL_SLICE_CHECKLIST.md) §3.1 (HUD metrics). **Design only** — no gameplay implementation yet; use this to confirm the sin/virtue console path and HUD are wired and testable.

---

## Reading Saved/pie_test_results.json

After running `pie_test_runner.py`, results are written to **`Saved/pie_test_results.json`**. Use this section to interpret the file.

**Top-level keys:**

| Key | Meaning |
|-----|--------|
| **pie_was_running** | `true` if PIE was active when the script ran; many checks require PIE. |
| **note** | Present when PIE was not running (e.g. "PIE not running. Start PIE first for full validation."). |
| **summary** | One-line result, e.g. `"25/35 passed"`. |
| **all_passed** | `true` only when every check passed. |
| **checks** | Array of per-check results (see below). |

**Each item in `checks`** has:

- **name** — Short label for the check (e.g. "PIE active", "Character spawned", "On ground").
- **passed** — `true` or `false`; use this for pass/fail.
- **detail** — Human-readable explanation (e.g. movement mode, actor count, or error message).

**Pass/fail:** A run is successful when `all_passed` is `true`. If not, inspect `checks` and look for entries with `"passed": false`; use **detail** to see why (e.g. "PIE not running", "No controlled pawn", "No actor with tag Portal_To_Planetoid").

**Check names (what they mean):**

| Check name | What it verifies |
|------------|------------------|
| PIE active | Play-in-Editor was running when the script ran. |
| Character spawned | Controlled pawn is present (e.g. BP_HomeWorldCharacter). |
| On ground | Character movement is not falling (on ground or walking). |
| Capsule dimensions | Character capsule has valid half-height and radius. |
| Skeletal mesh | Character mesh component has a skeletal mesh assigned. |
| AnimInstance | Character has an active animation instance. |
| PlaceActorClass set | BP_HomeWorldCharacter has a build-order class set for placement. |
| Placement API | GetPlacementHit/GetPlacementTransform (BuildPlacementSupport) works. |
| Place flow (PIE) | hw.PlaceWall and BuildOrder count (key P / placement). |
| Harvest flow (PIE) | TryHarvestInFront / Wood (E on harvestable). |
| PCG actors | Level has many static mesh actors (e.g. > 100 from PCG). |
| Portal configured | Level has an actor tagged Portal_To_Planetoid with LevelToOpen set. |
| Dungeon entrance configured | Level has an actor tagged Dungeon_POI. |
| Planetoid / HomesteadLandedOnPlanetoid | On planetoid/DemoMap, GameMode set HomesteadLandedOnPlanetoid. |
| Planetoid complete (hw.Planetoid.Complete) | After hw.Planetoid.Complete, GameMode bPlanetoidComplete is true (optional; see PLANETOID_HOMESTEAD.md §5). |
| TimeOfDay Phase 2 | After hw.TimeOfDay.Phase 2, GetIsNight() is true. |
| Astral death flow | hw.AstralDeath advances to Dawn and respawn. |
| Spirit ability (Phase 2) | hw.SpiritBurst at night runs without error. |
| Night collectible counters | Spiritual power/artefact counters readable at Phase 2. |
| ReportDeath | hw.ReportDeath and spirit roster. |
| Spirits (hw.Spirits) | Spirit roster count and IDs; run hw.ReportDeath then hw.Spirits (T5 / List 70). |
| GrantBossReward | hw.GrantBossReward and Wood. |
| Conversion test (hw.Conversion.Test) | ConvertedFoesThisNight and converted role. |
| Save/Load persistence | hw.Save / hw.Load and basic round-trip. |
| SaveGame round-trip (phase, LoveLevel, spiritual) | Phase, LoveLevel, spiritual power restored after save/load. |
| LoveLevel persistence (save/load) | LoveLevel restored after hw.Save then hw.Load. |
| TimeOfDay phase persistence (save/load) | Phase 2 (night) restored after save/load. |
| Spiritual power persistence (save/load) | Spiritual power count restored after save/load. |
| SpendSpiritualPower / astral HUD | hw.SpendSpiritualPower decreases power at Phase 2. |
| Day restoration (RestoreMeal) | hw.RestoreMeal sets day buff or meals count. |
| Day buff persistence (save/load) | Day buff still set after save/load. |
| Day buff bonus at night (collect) | At night, day buff gives +1 spiritual power per collect. |
| Love bonus at night (collect) | At night, LoveLevel adds bonus to spiritual collect. |
| Love task complete (hw.LoveTask.Complete) | hw.LoveTask.Complete increments LoveTasksCompletedToday and Love; MVP tutorial List 4 step 3. |
| Game with child complete (hw.GameWithChild.Complete) | hw.GameWithChild.Complete increments GamesWithChildToday and Love; MVP tutorial List 5 step 4. |

When PIE is running, the `checks` array in the JSON includes a result for **Planetoid complete (hw.Planetoid.Complete)** (optional flow; see [PLANETOID_HOMESTEAD.md](tasks/PLANETOID_HOMESTEAD.md) §5). Use the **name** field to match table rows above.

**One-line summary in Output Log:** When you run `pie_test_runner.py` from the Editor (MCP or Tools → Execute Python Script), the script also prints a one-line summary to the **Output Log** (e.g. "PIE validation: 25/35 passed" or "PIE validation failed: 25/35 (see Saved/pie_test_results.json)"). Use that for a quick pass/fail; use the JSON file for which checks failed and why.
